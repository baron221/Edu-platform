const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const lesson = await prisma.lesson.findFirst(); const { id, courseId, resources, ...data } = Object.assign(lesson, { resources: [] }); await prisma.lesson.update({ where: { id: lesson.id }, data }); console.log('success'); } catch(e) { console.error('Error:', e.message); } } main().finally(() => prisma.$disconnect());
